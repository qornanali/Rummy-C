#include "lib.h"

int main(int argc, char *argv[]) {
	srand(time(NULL));
//	menu_main(3,3);
//	getch();
//	PrepareCard();
//	addressCard card = First(card_on_deck);
//	while(card != NULL){
//		showmycard(Card(card),3,3);
//		slp(200);
//		cls();
//		card = Next(card);
//	}
infoCard card;
card.number = 1;
card.type = 1;
//showmycard(card,1,1);
	return 0;
}
